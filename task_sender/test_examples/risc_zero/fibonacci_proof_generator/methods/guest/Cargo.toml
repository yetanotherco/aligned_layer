[package]
name = "fibonacci"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
# If you want to try (experimental) std support, add `features = [ "std" ]` to risc0-zkvm
risc0-zkvm = { git = "https://github.com/risc0/risc0",  rev = "6107a65aa20816be9c62da0fa507794ebb1a682e", default-features = false }
